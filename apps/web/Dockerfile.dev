FROM node:24-alpine
WORKDIR /app
RUN corepack enable
COPY package.json pnpm-lock.yaml pnpm-workspace.yaml .npmrc ./
RUN pnpm install --frozen-lockfile
EXPOSE 3000
ENV HOST=0.0.0.0
ENV PORT=3000
CMD ["/app/node_modules/.bin/vite", "dev", "--host", "0.0.0.0", "--port", "3000"]
